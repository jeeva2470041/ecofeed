# Root env example (for local reference)
# Copy service-specific env examples to their folders and set real values.

# Backend (backend/.env):
# MONGODB_URI, JWT_SECRET, PORT, GROK_API_KEY

# Frontend (frontend/.env):
# VITE_API_URL
